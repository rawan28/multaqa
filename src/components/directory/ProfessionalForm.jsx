import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import ProfessionalPhotoUpload from "@/components/directory/ProfessionalPhotoUpload";
import CertificateUpload from "@/components/directory/CertificateUpload";

const initialValues = { full_name: "", profession: "psychologist", location: "", years_experience: "", appointment_mode: "both", specialty: "psychotherapy", license_number: "", work_days: [], accepting_new_patients: true, profile_image_url: "", website: "", accessibility: "", directions: "", phone: "", email: "", bio: "" };
const workDays = [["sunday", "الأحد"], ["monday", "الاثنين"], ["tuesday", "الثلاثاء"], ["wednesday", "الأربعاء"], ["thursday", "الخميس"], ["friday", "الجمعة"], ["saturday", "السبت"]];

export default function ProfessionalForm({ onSubmit }) {
  const [values, setValues] = useState(initialValues); const [certificates, setCertificates] = useState([]); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [message, setMessage] = useState("");
  const update = (event) => setValues({ ...values, [event.target.name]: event.target.value });
  const toggleDay = (day) => setValues({ ...values, work_days: values.work_days.includes(day) ? values.work_days.filter((item) => item !== day) : [...values.work_days, day] });
  const submit = async (event) => { event.preventDefault(); if (values.profession !== "psychologist" && !certificates.length) { setError("أرفق وثيقة اعتماد واحدة على الأقل."); return; } setSaving(true); setError(""); try { const result = await onSubmit({ profile: { ...values, years_experience: Number(values.years_experience) }, certificates }); setMessage(result.status === "approved" ? "تم تفعيل ملفك المهني." : "تم إرسال طلب التحقق للمراجعة."); setValues(initialValues); setCertificates([]); } catch { setError("تعذر إرسال الطلب. حاول مرة أخرى."); } finally { setSaving(false); } };
  return <form onSubmit={submit} className="grid gap-5"><ProfessionalPhotoUpload value={values.profile_image_url} onUpload={(profile_image_url) => setValues({ ...values, profile_image_url })} /><div><Label htmlFor="full_name">الاسم الكامل</Label><Input id="full_name" name="full_name" value={values.full_name} onChange={update} required /></div><div><Label htmlFor="profession">المهنة</Label><select id="profession" name="profession" value={values.profession} onChange={update} className="mt-2 h-10 w-full rounded-md border bg-background px-3 text-sm" required><option value="psychologist">أخصائي نفسي</option><option value="psychotherapist">معالج نفسي</option><option value="nlp">ممارس البرمجة اللغوية العصبية</option><option value="social_worker">أخصائي اجتماعي</option><option value="art_therapist">معالج بالفنون</option><option value="parent_coach">مدرب أهل / مرشد</option><option value="other">مهنة أخرى</option></select></div><div><Label htmlFor="location">الموقع</Label><Input id="location" name="location" value={values.location} onChange={update} required /></div><div className="grid gap-5 sm:grid-cols-2"><div><Label htmlFor="years_experience">سنوات الخبرة</Label><Input id="years_experience" name="years_experience" type="number" min="0" value={values.years_experience} onChange={update} required /></div><div><Label htmlFor="appointment_mode">نوع الجلسات</Label><select id="appointment_mode" name="appointment_mode" value={values.appointment_mode} onChange={update} className="mt-2 h-10 w-full rounded-md border bg-background px-3 text-sm" required><option value="online">عبر الإنترنت</option><option value="in_person">حضوري</option><option value="both">عبر الإنترنت وحضوري</option></select></div></div>{values.appointment_mode !== "online" && <div className="grid gap-5"><div><Label htmlFor="accessibility">إتاحة المكان</Label><Input id="accessibility" name="accessibility" value={values.accessibility} onChange={update} required /></div><div><Label htmlFor="directions">كيفية الوصول إلى المكان</Label><Textarea id="directions" name="directions" value={values.directions} onChange={update} className="mt-2 min-h-24" required /></div></div>}<div><Label htmlFor="specialty">مجال الممارسة</Label><Input id="specialty" name="specialty" value={values.specialty} onChange={update} required /></div><div><Label htmlFor="license_number">رقم ترخيص الممارس</Label><Input id="license_number" name="license_number" value={values.license_number} onChange={update} required /></div>{values.profession !== "psychologist" && <CertificateUpload files={certificates} onChange={setCertificates} required />}{values.profession === "psychologist" && <p className="rounded-md bg-secondary p-3 text-sm text-secondary-foreground">يُفعّل ملف الأخصائي النفسي مباشرة بعد الإرسال.</p>}<fieldset><legend className="text-sm font-medium">أيام العمل <span className="text-muted-foreground">(اختيارية)</span></legend><div className="mt-2 flex flex-wrap gap-3">{workDays.map(([value, label]) => <label key={value} className="flex items-center gap-1 text-sm"><input type="checkbox" checked={values.work_days.includes(value)} onChange={() => toggleDay(value)} />{label}</label>)}</div></fieldset><label className="flex items-center gap-2 text-sm font-medium"><input type="checkbox" checked={values.accepting_new_patients} onChange={(event) => setValues({ ...values, accepting_new_patients: event.target.checked })} />بإمكاني استقبال متوجهين جدد</label><div className="grid gap-5 sm:grid-cols-2"><div><Label htmlFor="phone">رقم الهاتف</Label><Input id="phone" name="phone" type="tel" value={values.phone} onChange={update} /></div><div><Label htmlFor="email">البريد الإلكتروني</Label><Input id="email" name="email" type="email" value={values.email} onChange={update} /></div></div><div><Label htmlFor="bio">نبذة عن ممارستك</Label><Textarea id="bio" name="bio" value={values.bio} onChange={update} className="mt-2 min-h-28" /></div>{error && <p className="text-sm text-destructive" role="alert">{error}</p>}{message && <p className="text-sm text-primary" aria-live="polite" role="status">{message}</p>}<Button type="submit" disabled={saving} className="w-full">{saving ? "جارٍ إرسال الطلب…" : "إرسال الملف للتحقق"}</Button></form>;
}